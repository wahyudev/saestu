query camaru

ALTER TABLE `camaru` ADD `npsn_smta` VARCHAR(250) NULL DEFAULT NULL AFTER `asal_smta`;



ALTER TABLE `camaru` ADD `tahun_masuk` VARCHAR(4) NOT NULL AFTER `kab_smta`, ADD `tahun_lulus` VARCHAR(4) NOT NULL AFTER `tahun_masuk`, ADD `gelar` VARCHAR(20) NOT NULL COMMENT 'untu pasca' AFTER `tahun_lulus`, ADD `id_pendidikan_terakhir` INT NOT NULL COMMENT 'untuk pasca' AFTER `gelar`;

ALTER TABLE `camaru` ADD `id_jenis_pendaftaran` INT NULL DEFAULT NULL COMMENT 'referense ke siakad.senis_pendaftaran versi teks ada di jalur ' AFTER `jalur`;

ALTER TABLE `camaru` ADD `id_pendidikan_ayah` INT NULL DEFAULT NULL AFTER `peng_ayah`, ADD `id_pekerjaan_ayah` INT NULL DEFAULT NULL AFTER `id_pendidikan_ayah`, ADD `tgl_lahir_ayah` DATE NULL DEFAULT NULL AFTER `id_pekerjaan_ayah`, ADD `nik_ayah` VARCHAR(100) NULL DEFAULT NULL AFTER `tgl_lahir_ayah`, ADD `telp_ayah` VARCHAR(100) NULL DEFAULT NULL AFTER `nik_ayah`, ADD `id_status_ayah` INT NULL DEFAULT NULL AFTER `telp_ayah`;

ALTER TABLE `camaru` ADD `angka_penghasilan_ayah` INT NULL DEFAULT NULL AFTER `id_status_ayah`;

ALTER TABLE `camaru` ADD `id_pendidikan_ibu` INT NULL DEFAULT NULL AFTER `peng_ibu`, ADD `id_pekerjaan_ibu` INT NULL DEFAULT NULL AFTER `id_pendidikan_ibu`, ADD `tgl_lahir_ibu` DATE NULL DEFAULT NULL AFTER `id_pekerjaan_ibu`, ADD `nik_ibu` VARCHAR(100) NULL DEFAULT NULL AFTER `tgl_lahir_ibu`, ADD `telp_ibu` VARCHAR(100) NULL DEFAULT NULL AFTER `nik_ibu`, ADD `angka_penghasilan_ibu` INT NULL DEFAULT NULL AFTER `telp_ibu`, ADD `id_status_ibu` INT NULL DEFAULT NULL AFTER `angka_penghasilan_ibu`;

ALTER TABLE `camaru` ADD `nama_wali` VARCHAR(200) NULL DEFAULT NULL AFTER `id_status_ibu`, ADD `tgl_lahir_wali` DATE NULL DEFAULT NULL AFTER `nama_wali`, ADD `id_pendidikan_wali` INT NULL DEFAULT NULL AFTER `tgl_lahir_wali`, ADD `id_pekerjaan_wali` INT NULL DEFAULT NULL AFTER `id_pendidikan_wali`, ADD `angka_penghasilan_wali` INT NULL DEFAULT NULL AFTER `id_pekerjaan_wali`;

ALTER TABLE `camaru` DROP `nama_sekolah`, DROP `alamat_sekolah`, DROP `kabupaten_sekolah`, DROP `propinsi_sekolah`;

ALTER TABLE `camaru` CHANGE `regnon` `regnon` VARCHAR(7) CHARACTER SET utf8 COLLATE utf8_general_ci NULL DEFAULT NULL COMMENT 'tidak digunakan';

ALTER TABLE `camaru` ADD `id_agama` INT NULL DEFAULT NULL AFTER `jenis_masuk`, ADD `kelurahan` VARCHAR(255) NULL DEFAULT NULL AFTER `id_agama`, ADD `id_kecamatan` INT NULL DEFAULT NULL AFTER `kelurahan`, ADD `rt` VARCHAR(10) NULL DEFAULT NULL AFTER `id_kecamatan`, ADD `rw` VARCHAR(10) NULL DEFAULT NULL AFTER `rt`, ADD `kode_pos` VARCHAR(10) NULL DEFAULT NULL AFTER `rw`, ADD `telp_wali` VARCHAR(20) NULL DEFAULT NULL AFTER `kode_pos`, ADD `golongan_darah` VARCHAR(100) NULL DEFAULT NULL AFTER `telp_wali`, ADD `hobi` TEXT NULL DEFAULT NULL AFTER `golongan_darah`, ADD `nilai_sek` FLOAT NULL DEFAULT NULL AFTER `hobi`, ADD `nilai_uan` FLOAT NULL DEFAULT NULL AFTER `nilai_sek`, ADD `scan_ijazah` VARCHAR(255) NULL DEFAULT NULL AFTER `nilai_uan`, ADD `scan_skhu` VARCHAR(255) NULL DEFAULT NULL AFTER `scan_ijazah`, ADD `jumlah_saudara` INT NULL DEFAULT NULL AFTER `scan_skhu`, ADD `daya_listrik` INT NULL DEFAULT NULL AFTER `jumlah_saudara`, ADD `scan_kartu_masuk_pt` VARCHAR(200) NOT NULL AFTER `daya_listrik`, ADD `scan_surat_penghasilan` VARCHAR(200) NOT NULL AFTER `scan_kartu_masuk_pt`, ADD `scan_rekening_listrik` VARCHAR(200) NOT NULL AFTER `scan_surat_penghasilan`, ADD `scan_pembayaran_pdam` VARCHAR(200) NOT NULL AFTER `scan_rekening_listrik`, ADD `scan_pembayaran_pbb` VARCHAR(200) NOT NULL AFTER `scan_pembayaran_pdam`, ADD `scan_kk` VARCHAR(200) NOT NULL AFTER `scan_pembayaran_pbb`, ADD `scan_akte_kelahiran` VARCHAR(200) NOT NULL AFTER `scan_kk`, ADD `foto_rumah_depan` VARCHAR(200) NOT NULL AFTER `scan_akte_kelahiran`, ADD `foto_ruang_tamu` VARCHAR(200) NOT NULL AFTER `foto_rumah_depan`, ADD `foto_dapur` VARCHAR(200) NOT NULL AFTER `foto_ruang_tamu`, ADD `foto_keluarga` VARCHAR(200) NOT NULL AFTER `foto_dapur`, ADD `foto_kamar` VARCHAR(200) NOT NULL AFTER `foto_keluarga`, ADD `status_submit_data` VARCHAR(200) NOT NULL DEFAULT 'belum_submit' AFTER `foto_kamar`, ADD `tanggal_submit` TIMESTAMP NULL DEFAULT NULL AFTER `status_submit_data`, ADD `catatan_bak` TEXT NULL DEFAULT NULL AFTER `tanggal_submit`, ADD `validasi_data_pokok` ENUM('menunggu','valid','tidak_valid') NOT NULL DEFAULT 'menunggu' AFTER `catatan_bak`, ADD `nip_validator` VARCHAR(100) NULL DEFAULT NULL AFTER `validasi_data_pokok`, ADD `open_bayar_ukt` TIMESTAMP NULL DEFAULT NULL AFTER `nip_validator`, ADD `close_bayar_ukt` TIMESTAMP NULL DEFAULT NULL AFTER `open_bayar_ukt`, ADD `id_tagihan_uang_pangkal` VARCHAR(100) NULL DEFAULT NULL AFTER `close_bayar_ukt`, ADD `open_bayar_pangkal` TIMESTAMP NULL DEFAULT NULL AFTER `id_tagihan_uang_pangkal`, ADD `close_bayar_pangkal` TIMESTAMP NULL DEFAULT NULL AFTER `open_bayar_pangkal`, ADD `pakai_uang_pangkal` ENUM('tidak','ya') NOT NULL DEFAULT 'tidak' AFTER `close_bayar_pangkal`, ADD `jumlah_uang_pangkal` INT NOT NULL DEFAULT '0' AFTER `pakai_uang_pangkal`, ADD `validasi_penerima_beasiswa` INT NOT NULL DEFAULT '0' AFTER `jumlah_uang_pangkal`;
